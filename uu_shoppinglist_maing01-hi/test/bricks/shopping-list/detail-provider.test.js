import UuShoppinglist from "uu_shoppinglist_maing01-hi";
import { mount, wait } from "uu5g05-test";

describe(`UuShoppinglist.Bricks.ShoppingList.DetailProvider`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<UuShoppinglist.Bricks.ShoppingList.DetailProvider onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
